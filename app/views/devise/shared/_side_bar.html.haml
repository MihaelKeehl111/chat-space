.side-box
  .header
    .header__name
      = current_user.name
    = link_to "#", class: "header__icon" do
      = icon('fas', 'edit')
     = link_to edit_user_path(current_user), class: "header__icon" do
      = icon('fas','cog')
  .group
    .group__name
      groupname
    .group__message
      message
    .group__name
      groupname
    .group__message
      message